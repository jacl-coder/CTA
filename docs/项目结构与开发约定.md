# 项目结构与开发约定

## 当前目录

```text
backend/             Python src-layout 项目、锁文件、后端测试
  src/cta_risk/
    domain/          领域模型与金额语义
    position/        开平仓和批次核算
    valuation/       盈亏、权益、保证金和敞口
    risk/            阈值规则与状态转换
    settlement/      日盯市、跨日计算与收盘调度
    application/     用例编排与业务状态所有者
    api/             HTTP 输入输出、健康检查与静态托管
    config/          严格 YAML 与配置模型
    storage/         SQLite 事务和持久化
    marketdata/      外部行情接入、去重与补拉
    execution/       模拟执行适配
    simulator/       两个独立模拟源
    replay/          历史事件调度
    clock/           可控时间和交易日
    report/          冻结日结结果导出
    bootstrap.py     依赖装配
    cli.py           程序入口
frontend/            React/Vite 项目及锁文件
  src/
    app/             应用入口、路由
    api/             HTTP 客户端和生成类型
    hooks/           请求生命周期与状态同步
    features/        按业务功能组织页面，当前只有 overview
    styles/          全局样式
    test/            测试环境
configs/             外部运行配置
docs/                设计、开发约定、生成的接口契约
scripts/             导出契约、同步前端资源
packaging/           PyInstaller 入口与 spec
```

`domain`、`position`、`valuation`、`settlement` 已实现核算基础；`application/ledger.py`、`storage/sqlite.py`、`storage/migrations/001_ledger.sql` 和 `api/ledger.py` 已接通初始化、持久化及查询。`risk/rules.py`、`execution/simulated.py`、`application/trading.py`、`api/trading.py` 及第二个 SQL 迁移已接通价格驱动的模拟交易与风险事务。`domain/market.py`、`application/market.py`、`marketdata`、`simulator`、`api/market.py` 及第三个 SQL 迁移已接通双源 HTTP 行情、原始数据持久化、有界补数和有序风控，见[双源说明](双源行情与恢复.md)。`application/settlement.py`、`settlement/scheduler.py`、`report/exporter.py`、`api/settlement.py` 与第四个迁移已接通事务日结、跨日持久化和冻结日报。其他业务包按阶段加入实现。前端六个业务目录已建立，`app` 提供统一快照上下文，`hooks` 负责轮询，`api` 管理契约及写请求。

## 依赖方向

领域模型不导入 FastAPI、Pydantic、SQLite 或 YAML。持仓、估值、规则和清算依赖领域模型；应用层组合业务行为；HTTP、数据库、数据源适配层依赖应用端口，由 `bootstrap.py` 统一装配。只在确实存在 I/O 或替换需求时引入 Protocol，不创建通用全能 service 或工具包。

前端只调用 `/api`，不读取数据库、不计算权威盈亏、不决定风控放行。生成的 API 类型保存在 `frontend/src/api/schema.d.ts`；可复用组件在实际出现多个消费者时提取。

## 状态与金额

当前账本服务使用单个业务协程作为状态所有者，单个 Uvicorn worker 运行；有界队列顺序处理账本模式的内部成交，或交易模式的价格、订单与行情接入命令，SQLite 专用线程提交成功后才发布新快照。已接收成交在调用方取消等待后继续处理，重试使用同一成交编号。模拟执行和双源行情已接入同一应用入口，日结已复用它，后续场景回放继续复用它，不能另建可绕过风控的写路径。

首次建库的初始化成交带明确来源，业务配置冻结在数据库中。每次启动核对配置并从成交重算验证快照，不重复初始化；同一数据库禁止多个写进程。已支持配置会话内跨日；具体契约见[账本接口与持久化](账本接口与持久化.md)。

金融值以十进制字符串跨 API、配置和存储边界，计算使用 Decimal；界面仅格式化展示。手续费、保证金和日结的口径通过独立手算样例固定，不能用被测函数自身生成预期值。

## 测试与生成文件

- `backend/tests/unit` 测纯计算、状态转换和配置；`integration` 测 HTTP、数据库与恢复；当前真实 HTTP 与进程验收由 `scripts/demo_risk.py`、`scripts/demo_market.py`、`scripts/demo_settlement.py` 执行。
- 前端组件测试贴近组件，浏览器业务流程落入后续 `frontend/e2e`。
- `make check` 检查 Python、TypeScript 和 lint；`make test` 跑已有测试。
- `make api` 生成的 OpenAPI 与 TypeScript 类型需要提交；前端构建、后端 static 副本、虚拟环境、运行数据与发布目录不提交。
- 新增运行依赖必须在对应清单声明并更新锁文件；不要提前安装尚未使用的行情分析或数据科学依赖。

## 当前验收边界

工程骨架已验证配置、HTTP、前后端与打包；核算基础已验证持仓、估值和逐日盯市领域函数。SQLite、账户初始化、只读查询、事务回滚与重启恢复已接通，详见[计算样例](业务口径与计算样例.md)及[测试方案](测试方案.md)。模拟价格驱动的风控下单、风险日志与恢复已验证；双源中断补全与风险一致性已验证；两日收盘清算、跨日持久化与日报已通过真实进程验收；前端业务展示已采用统一 HTTP 快照轮询接入，替代早期 SSE 计划；最终完整交付尚未验收。
